# Verification

All projects must have one verification entrypoint.

Default:

```bash
make verify
```

The agent must run this command itself, read failures itself, and repair failures within the approved budget.

The human must not be asked to run commands or paste logs.
