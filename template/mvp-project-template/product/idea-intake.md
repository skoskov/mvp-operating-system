# Idea Intake

Idea:

User:

Problem:

Current workaround:

Why current workaround is bad:

Problem frequency / urgency:

Trigger to try MVP:

Main use case:

MVP promise:

First success moment:

MVP scope:

Must-have:

Explicit non-goals:

Post-launch backlog:

Constraints:

Launch target:
